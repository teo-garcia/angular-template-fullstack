PNPM_VERSION=$(node -p 'require("./package.json").packageManager?.split("@")[1] ?? "latest"')
npx -y "pnpm@${PNPM_VERSION}" exec tsc -p tsconfig.app.json --noEmit
exec npx -y "pnpm@${PNPM_VERSION}" lint-staged
